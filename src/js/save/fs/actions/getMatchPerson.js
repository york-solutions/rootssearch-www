/**
 * Load the full person for a selected match
 */
 
const FS = require('../fs');

module.exports = function(personId, matchId){
  return function(dispatch, getState){
    
    // Set the state to loading
    
    // Fetch the person
    
    // Save the person
    
    // Set the state to loaded
    
  };
};