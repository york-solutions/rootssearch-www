// TODO: we only have this because it's required to have a reducer for each
// initial store value property. Lets find a better way to handle this.
module.exports = function(state = {}, action){
  return state;
};